spring:
# 스프링 부트는 설정이 없으면 기본적으로 메모리 모드로 실행됨
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true //system.out으로 찍음
#          format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace